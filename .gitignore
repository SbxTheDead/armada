# Build output
/bin/
coverage.out

# Compiled module artifacts (build sources into modules/dist)
/modules/dist/*
!/modules/dist/.gitkeep
# Agent module cache
/armada-modules/

# Agent local state (contains the device API key)
armada-agent.json
*-state.json

# Env files
.env
